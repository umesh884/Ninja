def firstAndLastPosition(arr,n,k):
    first = -1
    last = -1
    for i in range(len(arr)):
        if( k == arr[i]):
            if(first == -1):
                first = i
            last = i
            
    return (first,last)
